# Phase 1 依赖（pip 安装，建议走清华镜像）
# 清华源：pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
#
# 注意：pyqlib 对 Python 版本敏感，本项目使用独立 conda 环境 (python 3.10)。

# --- 量化核心 ---
pyqlib>=0.9.6
lightgbm>=4.0,<5
xgboost>=2.0

# --- 数据获取 ---
akshare>=1.12
yfinance>=0.2.40

# --- 数据处理 / 存储 ---
pandas>=2.0,<3
numpy>=1.24,<2
pyarrow>=14
duckdb>=0.10
sqlalchemy>=2.0

# --- 特征 / 模型 / 评估 ---
scipy>=1.10
scikit-learn>=1.3
shap>=0.44

# --- 配置 / CLI / 报告 ---
pyyaml>=6.0
typer>=0.12
rich>=13
jinja2>=3.1

# --- 新闻 / LLM（Phase 2，已并入主流程）---
openai>=1.30          # 火山方舟 Ark（OpenAI 兼容）→ DeepSeek-V3.2 事件抽取
feedparser>=6.0       # RSS 新闻

# --- 通用 ---
requests>=2.31
lxml>=5.0
tqdm>=4.66
python-dateutil>=2.9
