# AI 投资日报 · {{ as_of }}

> 基于 LightGBM + Qlib 因子引擎。概率 = 「未来 {{ horizon }} 个交易日跑赢所在行业」的预测概率。
> 仅作研究用途，不构成投资建议。

{% if backtest %}
**最近回测摘要**
```
{{ backtest }}
```
{% endif %}

---

{% for c in cards %}
### {{ loop.index }}. {{ c.name }}（{{ c.code }}） · {{ c.industry }} / {{ c.market|upper }}

- **未来 {{ horizon }} 交易日跑赢行业概率**：`{{ "%.0f"|format(c.prob * 100) }}%`
- **评分**：`{{ c.score }}/100`
- **可信度**：`{{ c.confidence }}`（数据完整度 `{{ "%.0f"|format(c.data_completeness * 100) }}%`）

**原因**
{% for r in c.reasons %}- + {{ r }}
{% endfor %}
**风险**
{% for r in c.risks %}- - {{ r }}
{% endfor %}

---
{% else %}
_当日无满足阈值（概率 ≥ 阈值）的标的。可调低 settings.report.min_probability 或扩大股票池。_
{% endfor %}

---
*由 {{ project }} 自动生成。*
