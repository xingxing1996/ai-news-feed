# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# OS / IDE
.DS_Store
.idea/
.vscode/
.claude/

# quant 本地数据/产物（重，可重新生成；GHA 里用 artifact 存模型）
quant/data/
quant/config/settings.local.yaml

# 自动生成的 feed JSON（sources.json 是配置，保留）
feed.json
feedLastTime.json

# 本地推送脚本（含 token，勿提交）
push.sh
ms_deploy.json
